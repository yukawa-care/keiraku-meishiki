# 経絡命式（仮称）プロジェクト

## プロジェクト概要

東洋医学（経絡・五行・五臓六腑）と東洋占術（四柱推命・紫微斗数・算命学・易経）を融合した商用 Web アプリ。鍼灸師資格を持つ運営者ならではの「**五行を体質・健康に翻訳する**」という独自の差別化軸を持つ。

- **プロダクト名（仮）**: 経絡命式 / 五行歴 / 気脈鑑定（最終決定保留）
- **運営**: ゆかわ鍼灸マッサージ治療院（湯川研一）
- **ドメイン**: 未取得（仮: keiraku-meishiki.jp）
- **MVP リリース目標**: 2026年9月（90日）
- **課金モデル**: 単発鑑定 3,000〜5,000円 + サブスク 月額 1,980円

---

## アーキテクチャ ― 3層モデル

本プロダクトの設計上、最も重要な原則。**Claude（LLM）の創作は最後の整文層のみに閉じ込める**ことで、占術アプリ最大のリスクであるハルシネーションを構造的に封じる。

### Layer 1: 計算エンジン（決定論）
- 入力: 生年月日時 + 出生地（経度補正用）
- 出力: 命式データ（厳密に同じ入力に対して同じ出力）
- ライブラリ: `iztro`（紫微斗数）、自作の四柱推命/算命学エンジン、易経 64卦ロジック
- **絶対にここに LLM を入れない**

### Layer 2: 解釈ナレッジベース（湯川先生監修の正典）
- 構造化 DB に格納された解釈ブロック
- キー例: `日柱十干 × 通変星` / `五行バランス × 経絡` / `卦 × 爻`
- ブロック単位で記述・レビュー可能
- これがプロダクトの**知的財産そのもの**

### Layer 3: 文章統合層（Claude API）
- 入力: Layer 1 の計算結果 + Layer 2 から取得した該当ブロック群
- 役割: **整文・並べ替え・文脈調整のみ**
- 禁止: ブロック外の解釈追加、新規ファクトの創作
- システムプロンプトで厳格に制約する（後述）

---

## 技術スタック

| 領域 | 選定 | バージョン |
|---|---|---|
| フレームワーク | Next.js | 15 (App Router) |
| 言語 | TypeScript | 5.x（strict mode） |
| スタイル | Tailwind CSS | 4.x |
| UI コンポーネント | shadcn/ui | - |
| 認証 | Clerk | latest |
| DB | Supabase (PostgreSQL) | latest |
| ORM | Drizzle ORM | latest |
| 決済 | Stripe | latest |
| LLM | Anthropic Claude API | Claude Sonnet 4.6 |
| 紫微斗数計算 | iztro | 最新安定版 |
| 天文計算（節気） | astronomy-engine | 最新 |
| デプロイ | Vercel | - |
| エラー監視 | Sentry | - |
| 解析 | PostHog | - |

---

## ディレクトリ構造

```
keiraku-meishiki/
├── app/                     # Next.js App Router
│   ├── (marketing)/         # LP, 価格ページ
│   ├── (auth)/              # ログイン・登録
│   ├── (app)/               # 認証済みエリア
│   │   ├── dashboard/
│   │   ├── reading/         # 鑑定実行・閲覧
│   │   └── settings/
│   └── api/
│       ├── reading/         # 鑑定生成 API
│       ├── stripe/          # 決済 Webhook
│       └── webhooks/
├── src/
│   ├── engines/             # Layer 1: 計算エンジン
│   │   ├── shichu/          # 四柱推命
│   │   ├── ziwei/           # 紫微斗数（iztro ラッパー）
│   │   ├── sanmei/          # 算命学
│   │   ├── ekkyo/           # 易経
│   │   └── shared/          # 干支・節気・経度補正
│   ├── interpretation/      # Layer 2: 正典 DB アクセス層
│   │   ├── repositories/
│   │   └── selectors/       # 命式 → 該当ブロック抽出ロジック
│   ├── synthesis/           # Layer 3: Claude API 統合層
│   │   ├── prompts/
│   │   └── client.ts
│   ├── db/                  # Drizzle スキーマ
│   ├── lib/                 # ユーティリティ
│   └── components/          # UI コンポーネント
├── data/
│   └── canonical/           # 正典 DB の JSON/YAML マスタ
├── tests/
│   ├── engines/             # 計算エンジンの単体テスト（網羅率100%目標）
│   └── e2e/
└── docs/
    └── glossary.md          # ドメイン用語集
```

---

## コーディング規約

- **言語**: TypeScript strict mode、`any` 禁止（やむを得ない場合は `unknown` + type guard）
- **ファイル名**: kebab-case（`solar-term-calculator.ts`）
- **コンポーネント名**: PascalCase
- **関数命名**: 動詞始まり（`calculateDayPillar`、`fetchInterpretationBlocks`）
- **テスト**: Vitest、計算エンジンは網羅率100%
- **コメント**: 計算ロジックには「典拠（書名・著者・ページ）」を必ずコメント記載
- **コミット**: Conventional Commits（`feat:`, `fix:`, `refactor:` 等）

---

## 絶対遵守ルール

### 1. 計算エンジンに LLM を呼ばない
Layer 1 は完全に決定論。命式の値を LLM に「推測」させてはいけない。

### 2. Layer 3 のシステムプロンプトテンプレ
```
あなたは整文係です。以下の解釈ブロックのみを材料として、
ユーザーへの鑑定文を自然な日本語に統合してください。

【厳守事項】
- ブロック外の情報を追加しない
- 新たな占術的判断を行わない
- 「治る」「治療」「効く」「診断」等の医療類似表現を使わない
- 断定的予言（「必ず」「絶対に」）を使わない
- 100点中100点プラスαの文章を、最適な構造フレームワークで書く

【解釈ブロック】
{blocks}

【ユーザー情報】
{user_context}
```

### 3. 法令対応
- **あはき法第7条**: 広告制限は「広告」に対する規制。プロダクト内文章は広告ではないが、LP・SNS 発信は対象。プロダクト内でも安全側で「治る」「治療」表現は禁止。
- **特定商取引法**: 商用販売開始までに表記ページ必須。
- **個人情報保護法**: 生年月日・出生時刻は機微情報として扱う。暗号化保存、退会時24時間以内削除。
- **景品表示法**: 「100%当たる」等の誇大広告禁止。

### 4. データ保存
- 生年月日・出生時刻・氏名: AES-256 暗号化、key は環境変数（Vercel 上で管理）
- 鑑定結果（生成済み文章）: 平文可、ただしユーザー削除可能に
- ログ: 個人情報を含めない

### 5. 流派の明示
- 四柱推命: 子平派ベース（明示）
- 紫微斗数: iztro デフォルト（中州派系）
- 算命学: 高尾系（湯川先生確認後確定）
- 易経: 周易（占筮法は3コイン法をデフォルト、筮竹法を選択可）

---

## ドメイン用語集（必要に応じ参照）

| 用語 | 意味 |
|---|---|
| 命式（めいしき） | 四柱推命の出力。年月日時の干支表。 |
| 干支（かんし） | 十干 × 十二支の60組合せ。 |
| 通変星（つうへんせい） | 日干と他干の関係性10種（比肩・劫財・食神…）。 |
| 蔵干（ぞうかん） | 地支の中に蔵された天干。 |
| 十二運（じゅうにうん） | 干の運勢12段階（長生・沐浴…）。 |
| 節入り（せついり） | 二十四節気の各節の開始時刻。命式の月柱の境目。 |
| 命盤（めいばん） | 紫微斗数の出力。12宮の星配置図。 |
| 主星（しゅせい） | 紫微斗数の14の主要な星。 |
| 四化（しか） | 化禄・化権・化科・化忌の変動星。 |
| 陰占（いんせん） | 算命学の干支表。 |
| 陽占（ようせん） | 算命学の人体星図。 |
| 卦（か） | 易経の64基本パターン。 |
| 爻（こう） | 卦を構成する6本の横線。 |

---

## 経絡 × 五行 マッピング（差別化の核）

詳細は `data/canonical/meridian-five-elements.json` を参照。基本対応：

| 五行 | 五臓 | 六腑 | 経絡 |
|---|---|---|---|
| 木 | 肝 | 胆 | 足厥陰肝経 / 足少陽胆経 |
| 火 | 心 | 小腸 | 手少陰心経 / 手太陽小腸経 |
| 土 | 脾 | 胃 | 足太陰脾経 / 足陽明胃経 |
| 金 | 肺 | 大腸 | 手太陰肺経 / 手陽明大腸経 |
| 水 | 腎 | 膀胱 | 足少陰腎経 / 足太陽膀胱経 |

命式の五行バランス（旺・相・休・囚・死）から、該当する臓腑・経絡の養生提案を Layer 2 から引く設計。これがプロダクトの最大の独自性。

---

## 開発フロー

1. 機能開発前に `docs/` のドメインドキュメントを参照
2. 新規計算ロジック実装前にテストケースを先に書く（TDD）
3. 計算結果は既存の信頼できる占いサイトと最低3件突合（誤差時はコメントで原因を記録）
4. Layer 2 の解釈ブロック新規追加時は、必ず湯川先生のレビュー（PR コメント形式）
5. デプロイは main ブランチへのマージで Vercel が自動実行

---

## このプロジェクトでの Claude Code への期待

- 4つの占術の数学的・暦学的精度を**絶対に妥協しない**
- 計算ロジックには必ず典拠コメントを残す
- LLM を計算層に侵入させない（Layer 3 のみ使用）
- 商用プロダクト水準のコード品質（型安全、テスト、エラーハンドリング）
- 日本語ドメイン用語は正確に（誤訳・誤字は信頼性を失う）
